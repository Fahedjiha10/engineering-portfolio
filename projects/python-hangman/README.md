# Python Hangman

Runnable console game with external word loading, alphabetic single-letter validation, duplicate-guess prevention, mistake limits, masked display, and explicit win/loss handling.

```bash
python hangman.py
```

A sample `words.txt` is included because the original supporting list was missing.
